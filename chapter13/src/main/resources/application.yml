server:
  port: 8888
logging:
  path: logs
  file: userManager.log

spring:
  application:
    name: userManager
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/course?characterEncoding=UTF-8&&serverTimezone=GMT
    username: root
    password: 111111

mybatis:
  type-aliases-package: com.course.model
  mapper-locations:
    - mapper/*